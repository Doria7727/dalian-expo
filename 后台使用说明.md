# 大连工博会官网 · Gitee 后台管理与上线说明

本说明面向**非技术人员**，指导你如何把网站免费上线到 **Gitee Pages（国内访问稳）**，并通过浏览器**账号密码后台**修改内容（无需懂代码）。

> 路线说明：Gitee 没有像 Netlify 那样的一键登录服务，Decap CMS 要连 Gitee 改写内容，必须配一个 **Cloudflare Workers 代理**（免费、国内可访问）。下面第 4 步会带你部署它，代码已帮你写好（`gitee-oauth-proxy.js`）。

---

## 一、网站结构

```
2027大连工博会网站/
├─ index.html              首页（及其他多个 .html 栏目页）
├─ admin/                  内容管理后台
│  ├─ index.html           后台入口（访问 你的域名/admin）
│  └─ config.yml           后台字段配置（需改 3 处占位符）
├─ assets/
│  ├─ js/data.json         ★ 全部网站文字内容（后台就是编辑这个文件）
│  ├─ js/main.js           页面渲染脚本（不用改）
│  ├─ css/style.css        样式（不用改）
│  ├─ vendor/decap-cms.js  后台程序（已本地化，国内加载快）
│  └─ img/                 图片
├─ gitee-oauth-proxy.js    Cloudflare 代理代码（第 4 步部署）
└─ 后台使用说明.md          本文件
```

**核心结论**：上线后你改内容，绝大多数情况**只需在后台网页里填表单、点保存**，不用碰代码。

---

## 二、注册 Gitee 账号（免费）

1. 打开 https://gitee.com ，点右上角「注册」，用邮箱注册（你的 `1060200619@qq.com` 即可）。
2. 注册后建议完成**实名认证**（Gitee Pages 与 OAuth 应用对实名用户更稳）。

---

## 三、把网站代码推送到 Gitee（一次性）

你的文件夹**已初始化为 git 仓库**（本地已提交）。只需连上 Gitee：

1. 登录 Gitee，点右上角「+」→「新建仓库」。
2. 仓库名填 `dalian-expo-2027`（英文），选 **公开**，勾选「使用 Readme 初始化」**取消**（保持空仓库），点「创建」。
3. 复制仓库的 **HTTPS 地址**（形如 `https://gitee.com/你的用户名/dalian-expo-2027.git`）。
4. 在你电脑上打开「Git Bash」，进入网站文件夹，执行（把地址换成你的）：
   ```bash
   cd "C:/Users/ASUS/Desktop/2027大连工博会网站"
   git remote add origin https://gitee.com/你的用户名/dalian-expo-2027.git
   git branch -M main
   git push -u origin main
   ```
   推送时输入 Gitee 用户名和密码（或私人令牌 Token）。
5. 刷新 Gitee 仓库页面，能看到所有文件即成功。

> 若你不会用 Git 命令：可在 Gitee 新建仓库后，用「文件」→「上传文件」把文件夹内容拖进去（多层目录需逐个上传，较麻烦，推荐用上面的命令）。

---

## 四、部署 Cloudflare 代理（免费，让后台能登录 Gitee）

1. 打开 https://dash.cloudflare.com ，用邮箱注册/登录（免费）。
2. 左侧菜单「Workers 和 Pages」→「创建」→「Worker」→ 名称随便（如 `dalian-expo-proxy`）→「部署」。
3. 部署后点「编辑代码」，把本仓库根目录 **`gitee-oauth-proxy.js`** 的**全部内容**粘贴进去，保存并部署。
4. 记下你的 Worker 地址，形如 `https://dalian-expo-proxy.你的子域.workers.dev`。
5. 在 Worker 页面点「设置」→「变量」→ 添加 3 个**环境变量**（注意选加密/Secret 保存）：
   - `GITEE_CLIENT_ID` = 你第 5 步建的 Gitee OAuth 应用的 Client ID
   - `GITEE_CLIENT_SECRET` = 对应的 Client Secret
   - `REDIRECT_URI` = `https://dalian-expo-proxy.你的子域.workers.dev/callback`
6. 改好变量后**重新部署一次** Worker 使变量生效。

---

## 五、在 Gitee 创建 OAuth 应用

1. Gitee 右上角头像 →「设置」→ 左侧「第三方应用」→「创建应用」。
2. 应用名称：`dalian-expo-cms`（随便填）。
3. **主页 URL**：填你后面第 6 步拿到的 Gitee Pages 地址（先留 `https://gitee.com` 也可，建好后再改）。
4. **回调地址**：填 `https://dalian-expo-proxy.你的子域.workers.dev/callback`（与第 4 步 REDIRECT_URI 完全一致）。
5. 权限勾选 `projects`（读写仓库内容即可）。
6. 提交后，记下 **Client ID** 和 **Client Secret**（Secret 只显示一次，复制保存）。
7. 把这两个值填回第 4 步的 Worker 环境变量并重新部署。

---

## 六、开启 Gitee Pages（官网正式上线）

1. 进入你的 Gitee 仓库 →「服务」→「Gitee Pages」。
2. 部署分支选 `main`，部署目录选「根目录 `/`」，点「启动」。
3. 约 1 分钟后得到官网地址，形如 `https://你的用户名.gitee.io/dalian-expo-2027/`。
   - 这就是你的正式对外网址，国内访问快，可发朋友圈/群里。

---

## 七、让后台连上（改 config.yml 三处占位符）

打开 `admin/config.yml`，把顶部 backend 段的三处占位符改成你的真实值：

```yaml
backend:
  name: gitee
  repo: 你的Gitee用户名/dalian-expo-2027          # 改成你的 用户名/仓库名
  auth_endpoint: https://dalian-expo-proxy.你的子域.workers.dev   # 改成你的 Worker 地址
  app_id: 你的Gitee_OAuth_Client_ID               # 改成第 5 步的 Client ID
  branch: main
```

保存后，回到 Git Bash 重新推送：
```bash
git add -A
git commit -m "配置 Gitee 后台"
git push
```
Gitee Pages 会自动重新发布（约 1 分钟）。

---

## 八、日常改内容（每次都这样）

1. 浏览器打开：`你的GiteePages地址/admin`（例如 `https://用户名.gitee.io/dalian-expo-2027/admin`）。
2. 点登录 → 跳到 Gitee 授权页 → 点「授权」。
3. 左侧「网站内容 → 全部内容 (data.json)」，点开就是一张大表单：
   - 展会基本信息（名称、时间、地点、电话、邮箱…）
   - 导航菜单 / 展会介绍 / 展品范围（展区）
   - 展商名录（可增删企业）/ 新闻动态（可增删新闻）
   - 同期活动 / 交通指南 / 酒店指南 / 参展报名信息
4. 改完点右上角 **Publish**（发布）。
5. 等约 1 分钟，刷新前台即可看到更新。

> 列表字段点下方的「+」新增一条，点「×」删除。

---

## 九、关于域名（.com / .cn）

- 当前用 Gitee Pages 免费地址（`*.gitee.io`），**够用、国内快、零成本**。
- 想用正式品牌域名（如 `dalian-expo.cn`，之前查过仍可注册，约 30–70 元/年）：
  - 在阿里云/腾讯云注册，做 DNS 解析指向 Gitee Pages（Gitee Pages 支持绑定自定义域名）。
  - 注意：`.cn` 需实名认证；若指向**大陆境内服务器**做网站，还需办 ICP 备案。

---

## 十、常见问题

**Q：后台登录后空白/报跨域错误？**
A：检查 ① Worker 的 3 个环境变量是否填对并重新部署；② Gitee OAuth 应用的回调地址 = Worker 的 `/callback`；③ config.yml 的 `auth_endpoint` 与 Worker 地址一致。

**Q：改了后台，前台没变？**
A：等 1–2 分钟（Gitee Pages 重新发布需要时间）；或硬刷新（Ctrl+F5）。

**Q：手机能改吗？**
A：能。`你的地址/admin` 在手机浏览器同样可用。

**Q：数据安全吗？**
A：所有内容保存在你的 Gitee 仓库里（有历史版本），误改可在 Gitee 上回滚。
