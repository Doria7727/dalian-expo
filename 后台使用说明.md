# 大连工博会官网 · 后台管理与上线说明

本说明面向**非技术人员**，指导你如何把网站免费上线、并通过浏览器账号密码后台修改内容（无需懂代码）。

---

## 一、网站现在的结构

```
2027大连工博会网站/
├─ index.html              首页（及其他多个 .html 栏目页）
├─ admin/                  内容管理后台
│  ├─ index.html           后台登录入口（访问 你的域名/admin）
│  └─ config.yml           后台字段配置（一般不用改）
├─ assets/
│  ├─ js/
│  │  ├─ data.json         ★ 全部网站文字内容（后台就是编辑这个文件）
│  │  └─ main.js           页面渲染脚本（一般不用改）
│  ├─ css/style.css        样式（一般不用改）
│  └─ img/                 图片
├─ netlify.toml            部署配置（不用改）
└─ 后台使用说明.md          本文件
```

**核心结论**：你以后改内容，绝大多数情况**只需在后台网页里填表单、点保存**，不用碰任何代码文件。

---

## 二、免费上线（一次性，约 10 分钟）

### 第 1 步：注册 GitHub 账号（免费）
1. 打开 https://github.com ，点 Sign up，按提示注册（用邮箱即可）。
2. 记下账号和密码，后面要用。

### 第 2 步：把网站文件夹上传到 GitHub（一次性）
> 这一步如果觉得麻烦，可以让我（AI 助手）帮你做；此处给手动做法。

1. 登录 GitHub，点右上角 “+” → New repository。
2. 仓库名填 `dalian-expo-2027`（英文，随便起），选 **Public**，点 Create repository。
3. 在仓库页面点 “uploading an existing file”，把 `2027大连工博会网站` 文件夹里的**全部内容**拖进去，点 Commit changes。
4. 现在你的代码已经在 GitHub 上了。

### 第 3 步：用 Netlify 导入并免费上线
1. 打开 https://app.netlify.com ，用 **GitHub 账号登录**。
2. 点 “Add new site” → “Import an existing project” → 选 GitHub → 授权 → 选中 `dalian-expo-2027` 仓库。
3. Build command 留空，Publish directory 填 `.`（点号），点 Deploy。
4. 约 30 秒后，Netlify 给你一个免费子域名，例如 `https://xxx.netlify.app`。
   - 这就是你的正式对外网址，可以马上发朋友圈/群里。

### 第 4 步：开启“账号密码后台”（关键）
1. 在 Netlify 站点后台，左侧菜单点 **Identity** → **Enable Identity**（启用身份服务）。
2. 在 Identity 设置里，Registration（注册方式）选 **Invite only**（仅邀请），或 Open（开放，方便自己先试用）。
3. 同页面下方点 **Services** → **Enable Git Gateway**（启用 Git 网关，后台才能提交保存）。
4. 在 Identity 页点 **Invite users**，输入你自己的邮箱，发出邀请。
5. 去该邮箱查收邀请邮件，点链接设置你的**登录密码**。

---

## 三、日常改内容（每次都这样）

1. 浏览器打开：`你的域名/admin`（例如 `https://xxx.netlify.app/admin`）。
2. 用刚才设置的邮箱 + 密码登录。
3. 左侧看到 “网站内容 → 全部内容 (data.json)”，点开就是一张大表单：
   - 展会基本信息（名称、时间、地点、电话、邮箱…）
   - 导航菜单
   - 展会介绍
   - 展品范围（展区）
   - 展商名录（可增删企业）
   - 新闻动态（可增删新闻）
   - 同期活动 / 交通指南 / 酒店指南 / 参展报名信息
4. 改完点右上角 **Save / Publish**（保存并发布）。
5. 等待约 1 分钟，刷新前台网站即可看到更新。

> 提示：新闻、展商都是“列表”，点字段下方的 “+” 即可新增一条，点 “×” 删除。

---

## 四、如果你不想用后台，也能直接改文件

不开启后台也行：用记事本打开 `assets/js/data.json`，按 JSON 结构改对应文字，保存后重新把文件夹拖到 Netlify（Deploy manually）覆盖即可。
（注意 JSON 格式：字符串要用英文双引号，逗号不能多也不能少。）

---

## 五、关于域名（.com / .cn）

- 当前用的是 Netlify 免费子域名（`.netlify.app`），**够用了，零成本**。
- 想用正式品牌域名（如 `dalian-expo.cn`）：
  - 在阿里云/腾讯云/西部数码等注册（约 30–70 元/年）。
  - 在 Netlify 站点后台 **Domain settings → Add custom domain** 绑定即可，免费子域名继续可用。
  - 注意：`.cn` 需实名认证；若指向**大陆境内服务器**做网站，还需办 ICP 备案（约 1–2 周，免费）。
- 之前查过：`dalian-expo.com` 已被注册；`dalian-expo.cn` 仍可注册。

---

## 六、常见问题

**Q：后台登录后空白/报错？**
A：确认第 4 步的 **Git Gateway 已 Enable**，且邀请的邮箱已完成密码设置。

**Q：改了后台，前台没变？**
A：等 1–2 分钟（Netlify 重新发布需要时间）；或硬刷新浏览器（Ctrl+F5）。

**Q：手机能改吗？**
A：能。`你的域名/admin` 在手机浏览器同样可用，响应式适配。

**Q：数据安全吗？**
A：所有内容保存在你的 GitHub 仓库里（有历史版本），误改可回滚。
